# Whop Webhook URL

Use this exact URL when creating the webhook in the Whop dashboard:

```
https://para-shipped-nam-travel.trycloudflare.com/api/whop/webhook
```

Notes:
- Paste it exactly as-is, including `https://` and the full `/api/whop/webhook` path.
- It is a public HTTPS endpoint (Cloudflare tunnel to `localhost:3000`).
- No Vercel deployment is required for testing.
